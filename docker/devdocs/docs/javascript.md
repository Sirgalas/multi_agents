# Modern JavaScript (ES2024+) Conventions

- **Language Features**: Use modern syntax (const/let, arrow functions, destructuring, rest/spread operators).
- **Async Concurrency**: Handle async workflows with async/await and Promise.allSettled() for fault-tolerant parallel executions.
- **Safe Access**: Utilize optional chaining (?.) and nullish coalescing (??) for resilient property access.
- **Modules**: Standardize on ES Modules (import/export).
- **Data Processing**: Prefer immutable array methods (map, filter, toSorted, toReversed, flatMap).
